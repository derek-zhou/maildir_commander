{erl_opts, [debug_info]}.
{deps, [iconv,
	gen_smtp]}.

{shell, [
  % {config, "config/sys.config"},
    {apps, [maildir_commander]}
]}.

{relx, [{release, {maildir_commander, "0.1.1"}, [maildir_commander]},
	{mode, minimal},
        {extended_start_script, true}]}.
